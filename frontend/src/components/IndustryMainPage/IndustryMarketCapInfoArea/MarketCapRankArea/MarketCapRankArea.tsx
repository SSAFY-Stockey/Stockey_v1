import styled from "styled-components"

const MarketCapRankArea = () => {
  return <AreaDiv>MarketCapRankArea</AreaDiv>
}

export default MarketCapRankArea

const AreaDiv = styled.div`
  flex-grow: 1;
  height: auto;
`
